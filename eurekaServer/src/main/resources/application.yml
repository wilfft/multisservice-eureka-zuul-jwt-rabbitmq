server:
  port: 8761
eureka:
 instance:
   hostname: localhost
 client:
     register-with-eureka: false
     fetch-registry: false
     serviceUrl:
       defaultZome: hht://${eureka.instance.hostname:${server.port}/eureka/
 server:
   eviction-interval-timer-in-ms: 1000